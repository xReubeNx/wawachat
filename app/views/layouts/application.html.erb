<!DOCTYPE html>
<html>
  <head>
    <title>Wawachat</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="flex flex-col items-center bg-orange-200 h-screen">
      <div class="border-b border-black/60 w-screen px-20 mb-1 bg-orange-300/20 h-[14%] shadow flex align-center place-content-between">
        <a href="/" class="text-blue-800 text-5xl font-bold leading-normal mt-4">Wawachat</a>

        <% if @user&.present? %>
          <div class="flex flex-col items-center">
          <%= @user.username %>
          <%= image_tag "user.png", class: "h-12 hover:cursor-pointer" %>
          <a href="/logout">Logout</a>
          </div>
        <% end %>
      </div>
      <div class="sm:w-2/5 w-full border border-black/60 rounded-xl bg-orange-100 h-[85%] p-2">
      <%= yield %>
      </div>
    </main>
  </body>
</html>
